<%@ page language="java" contentType="text/html; charset=ISO-8859-1"
    pageEncoding="ISO-8859-1" import="edu.neu.cs5200.jdbc.ds.dao.*, edu.neu.cs5200.jdbc.ds.model.*, java.sql.*,java.util.List"%>
<!DOCTYPE html PUBLIC "-//W3C//DTD HTML 4.01 Transitional//EN" "http://www.w3.org/TR/html4/loose.dtd">
<html>
<head>
<meta http-equiv="Content-Type" content="text/html; charset=ISO-8859-1">
<title>Insert title here</title>
</head>
<body>
	<h1>Hello World</h1>
        <%
            /* UserDAO userDAO = new UserDAO();
        	
        	User user = new User();
        	user.setUsername("cs5200");
        	user.setPassword("5555");
        	user.setLastname("aaaa");
        	user.setFirstname("bbbb");
        	user.setEmail("hot");
        	
        	Date d = new Date(2005/8/24);
        	
        	user.setDateofbrith(d);
        	
        	
   			userDAO.create(user);     */
   			
   			/* List<User> users = userDAO.findAllUsers();
   			
   			for(User u : users)
   			{
   				out.println(u.getUsername());
   			}
   
   			User user = userDAO.findById(1);
   			out.println(user.getUsername()); 
   			
   			user.setUsername("Jay Chow");
   			userDAO.updateUser(1, user);
   			
   			user = userDAO.findById(1);
   			out.println(user.getUsername());
   			
   			userDAO.deleteUser(3); */
   			
   			
   			
   			//CommentDAO commentDAO = new CommentDAO();
   			
   			/* Comment comment = new Comment();
   			comment.setComment("This is the comment");
   			
   			Date d = new Date(2015/3/28);
   			comment.setDate(d); 
   			
   			commentDAO.create(comment); */
   			
		/*  List<Comment> comments = commentDAO.findAllComments();
   			
   			for(Comment c : comments)
   			{
   				out.println(c.getComment());
   			}
   */
   			/* Comment comment = commentDAO.findById(1);
   			out.println(comment.getComment());
   			
   			comment.setComment("comment123");
   			commentDAO.updateComment(1, comment);
   			
   			comment = commentDAO.findById(1);
   			out.println(comment.getComment());
   			commentDAO.deleteComment(1); */
   			
   			 
   			 
   			
   			
        %>



</body>
</html>